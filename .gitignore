# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Data files — raw GH Archive hours never belong in git
*.json.gz
raw_files/
_download_audit.jsonl

# OS / editor
.DS_Store
.idea/
.vscode/

# Raw per-hour schema profiles from pipelines/schema_drift/scan_decade.py.
# ~2.4 MB per sampled hour (every field path with its distinct values), so
# a decade sample runs to hundreds of MB. Regenerate with scan_decade.py;
# commit the derived timeline and outage map instead.
artifacts/schema_drift/hourly_profiles.jsonl
artifacts/schema_drift/weekly_2025_2026.jsonl
